{
  "name": "appsbox-conv-documentos",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "tsc",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "jsdom": "^30.0.1",
    "typescript": "^5.7.3"
  }
}
