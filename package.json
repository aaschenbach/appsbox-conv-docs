{
  "name": "appsbox-conv-documentos",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "tsc",
    "test": "node --test tests/*.test.mjs",
    "build-afm": "node scripts/build-afm.mjs",
    "generate-seo": "npm run build && node scripts/generate-seo-pages.mjs"
  },
  "devDependencies": {
    "jsdom": "^30.0.1",
    "typescript": "^5.7.3"
  }
}
